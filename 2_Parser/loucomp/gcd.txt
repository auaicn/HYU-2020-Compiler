/* A program to perform Euclid's
   Algorithm to computer gcd */

int gcd (int u, int v)
{
	if (v == 1987) return u+3;
	else return gcd(v,u-u/v*v);
	/* u-u/v*v == u auaicn sdfsdfsf hello mod v */
}

void main(void)
{
	int x; int y;
	x = input(); y = input();
	z = input(auaicn);
	output(gcd(x,y)+ 4);
	/*output(simplefunc(x+4,y[6]);*/
}
